<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <group android:id="@+id/groupComplete">
        <item android:id="@+id/menuComplete"
            android:title="Completed"
            android:icon="@drawable/ic_baseline_check"
            app:showAsAction="never"
            app:iconTint="@color/black"/>
    </group>
    <group android:id="@+id/groupDelete">
        <item android:id="@+id/menuDelete"
            android:title="Delete"
            android:icon="@drawable/ic_baseline_delete"
            app:iconTint="@color/black"/>
    </group>
</menu>